PySide6>=6.5.0
requests>=2.28.0
paramiko>=3.0.0
astropy>=5.0
numpy>=1.22
astroalign>=2.4
pandas>=1.5
photutils>=1.5
astroquery>=0.4.6
matplotlib>=3.5
# Optional – improves stacking quality but not required:
# sep>=1.2.1        (faster source extraction for astroalign)
# reproject>=0.9    (WCS-based alignment fallback when star registration fails)
